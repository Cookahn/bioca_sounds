package com.fathead.chicobiocaapp;import java.io.File;import android.app.Activity;import android.content.Context;import android.content.Intent;import android.media.MediaPlayer;import android.os.Bundle;import android.os.Environment;import android.os.Handler;import android.view.KeyEvent;import android.view.Menu;import android.view.View;import android.view.View.OnClickListener;import android.view.animation.Animation;import android.view.animation.AnimationUtils;import android.widget.Button;import android.widget.ImageView;public class MainActivity extends Activity {	int clickCount = 0;	boolean pass = false;	Button toFuck, secretButton;	ImageView negoBam;	Animation negoSur, negoRoda, negoFlip;	Handler handler = new Handler();	MediaPlayer undefined;	int soundBam = 0;	Context context;		@Override	protected void onCreate(Bundle savedInstanceState) {		super.onCreate(savedInstanceState);		setContentView(R.layout.activity_main);		createFolder();		negoBam = (ImageView) findViewById(R.id.nego_bam_secret);		toFuck = (Button) findViewById(R.id.i_want_to_fuck);		secretButton = (Button) findViewById(R.id.secret_button_bioca);		final MediaPlayer mp = MediaPlayer.create(this,				R.raw.transa_nessa_porra);		negoSur = AnimationUtils.loadAnimation(this, R.anim.nego_anim);		negoFlip = AnimationUtils.loadAnimation(this, R.anim.grow_from_middle);		negoRoda = AnimationUtils.loadAnimation(this, R.anim.rot_nego);		toFuck.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				if(soundBam == 1) {				undefined.stop();				mp.start();				negoBam.clearAnimation();				negoBam.setVisibility(View.GONE);				Intent intent = new Intent(MainActivity.this,						RemixActivity.class);				startActivity(intent);				soundBam = 0;				} else {				mp.start();				Intent intent = new Intent(MainActivity.this,						RemixActivity.class);				startActivity(intent);				}			}		});		secretButton.setOnClickListener(new OnClickListener() {			@Override			public void onClick(View v) {				clickCount += 1;				if (clickCount == 10) {					pass = true;					negoBam.setVisibility(View.VISIBLE);					negoBam.startAnimation(negoSur);					negoBam.startAnimation(negoRoda);					undefined = MediaPlayer.create(MainActivity.this,							R.raw.gozar_your_ass);					undefined.start();					clickCount = 0;					soundBam = 1;				} else if (clickCount == 1 && pass) {					negoBam.clearAnimation();					negoBam.setVisibility(View.GONE);					undefined.stop();				}			}		});	}	@Override	public boolean onKeyDown(int keyCode, KeyEvent event) {		if ((keyCode == KeyEvent.KEYCODE_BACK)) { 			if(soundBam == 1){			undefined.stop();			soundBam = 0;			}			System.exit(0);			return true;		}		return super.onKeyDown(keyCode, event);	}		@Override    protected void onStop()     {        super.onStop();		if(soundBam == 1) {        undefined.stop();        negoBam.clearAnimation();        negoBam.setVisibility(View.GONE);		soundBam = 0;		}    }	@Override	public boolean onCreateOptionsMenu(Menu menu) {		getMenuInflater().inflate(R.menu.main, menu);		return true;	}			public void createFolder() {		File folder = new File(Environment.getExternalStorageDirectory()				+ "/.biocas");		if (!folder.exists()) {			folder.mkdir();			System.out.println("aquuiii");					}	}}